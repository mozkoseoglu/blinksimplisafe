modes = dict(
    use_arm_sync=True,
    use_battery_check=True
)
blink = dict(
    username='YOUR_BLINK_USER',
    password='YOUR_BLINK_PW',
    system_name='YOUR_BLINK_SYSTEM_NAME',
    refresh_rate=30,
    battery_threshold=50
)
simplisafe = dict(
    username='YOUR_SIMPLISAFE_USER',
    password='YOUR_SIMPLISAFE_PW',
    address='Your address from the SimpliSafe Homepage'
)
logs = dict(
    log_path='./'
)
schedule = dict(
    use_schedule=True,
    check_interval_in_s=60
)